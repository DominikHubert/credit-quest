version: '3.8'

services:
  credit-quest:
    image: nutrition/tracker
    ports:
      - "3005:80"
    volumes:
      - credit_data:/app/data
    container_name: credit-quest
    restart: unless-stopped

volumes:
  credit_data:
